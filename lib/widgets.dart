import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFFA4D8E9);
  static const Color blue = Color(0xFF143FFC);
  static const Color white = Colors.white;
}

class AppTheme {
  static const double cardPadding = 25;
  static BorderRadius cardRadius = BorderRadius.circular(25);
  static BorderRadius elementSpacing = BorderRadius.circular(25);
}
